name: CI Simple
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v3
      - name: Ejecutar script Python
        run: mi-workflow_AZB-script_prueba.py
